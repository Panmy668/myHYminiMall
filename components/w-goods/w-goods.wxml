<!--components/w-goods/w-goods.wxml-->

<view class="goods">
<block wx:for="{{goods}}" wx:key="{{index}}">
 <w-goodsitem class="goodsItem" goodsItem="{{item}}" />
</block>

 <view class="goodsItem" wx:if="{{goods.length%2!=0}}">
	</view>
</view>